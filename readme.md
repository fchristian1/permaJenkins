# This is a guide to install Jenkins with Docker on a local machine.

jenkins in a dcoker container.
backup and restore jenkins data periodically.
a websocket client connection to an host in the internet to receive wenhooks from github.
a websocket server to recieve the webhocks from github and send them to the local websocket client to trigger the jenkins job.